# 🖥️ ЛОКАЛЬНАЯ РАЗРАБОТКА - ПОЛНОЕ РУКОВОДСТВО

**Цель:** Разработка и тестирование только на локальном MacBook. Деплой на production только после успешных тестов и явной команды.

---

## 🎯 ПРИНЦИПЫ РАБОТЫ

1. ✅ **По умолчанию - только локально**
   - SQLite база данных (`./data/history.db`)
   - Локальный сервер на `localhost:8000`
   - Локальный frontend на `localhost:3000`
   - Никаких внешних зависимостей (Supabase, Railway, Vercel)

2. ✅ **Деплой только по команде**
   - После успешных локальных тестов
   - Только после явной команды `./deploy_production.sh`
   - Никогда автоматически

3. ✅ **Любой может установить и работать**
   - Простая установка за 5 минут
   - Минимум зависимостей
   - Работает из коробки

---

## 🚀 БЫСТРАЯ УСТАНОВКА (5 минут)

### Шаг 1: Клонирование репозитория

```bash
git clone <your-repo-url>
cd autopilot-core
```

### Шаг 2: Настройка Backend

```bash
# Создать виртуальное окружение
python3 -m venv venv
source venv/bin/activate

# Установить зависимости
pip install -r requirements.txt

# Создать .env файл
cp .env.example .env

# Сгенерировать SECRET_KEY
python -c "import secrets; print(secrets.token_urlsafe(32))"
# Скопируйте вывод и вставьте в .env:
# SECRET_KEY=ваш-сгенерированный-ключ

# Установить ENVIRONMENT=development (по умолчанию)
echo "ENVIRONMENT=development" >> .env
```

### Шаг 3: Настройка Frontend

```bash
cd web-ui

# Установить зависимости
npm install

# Создать .env.local для локальной разработки
cat > .env.local << EOF
NEXT_PUBLIC_API_URL=http://localhost:8000
EOF
```

### Шаг 4: Запуск

**Терминал 1 - Backend:**
```bash
cd ~/autopilot-core
source venv/bin/activate
python api/server.py
```

**Терминал 2 - Frontend:**
```bash
cd ~/autopilot-core/web-ui
npm run dev
```

**Откройте браузер:** http://localhost:3000

---

## 📁 СТРУКТУРА ЛОКАЛЬНОЙ РАЗРАБОТКИ

```
autopilot-core/
├── .env                    # Локальные переменные окружения
├── data/
│   └── history.db         # SQLite база данных (создается автоматически)
├── venv/                  # Python виртуальное окружение
├── web-ui/
│   ├── .env.local        # Frontend переменные (только локальные)
│   └── node_modules/      # Frontend зависимости
└── logs/                  # Логи (создается автоматически)
```

---

## ⚙️ КОНФИГУРАЦИЯ ДЛЯ ЛОКАЛЬНОЙ РАЗРАБОТКИ

### `.env` файл (Backend)

```bash
# ============================================
# ЛОКАЛЬНАЯ РАЗРАБОТКА - НЕ МЕНЯТЬ ДЛЯ PRODUCTION
# ============================================

# Обязательно: Environment
ENVIRONMENT=development

# Обязательно: JWT Secret
SECRET_KEY=ваш-сгенерированный-ключ-64-символа

# Обязательно: API Keys (минимум один)
GEMINI_API_KEY=your-key-here
# или
OPENROUTER_API_KEY=your-key-here
# или
ANTHROPIC_API_KEY=your-key-here

# Локальная база данных (SQLite)
DATABASE_PATH=./data/history.db

# Локальный сервер
HOST=0.0.0.0
PORT=8000

# Frontend URL (локальный)
FRONTEND_URL=http://localhost:3000
BASE_URL=http://localhost:3000

# CORS (только локальные домены)
CORS_ORIGINS=http://localhost:3000,http://localhost:3001

# ============================================
# НЕ НУЖНО ДЛЯ ЛОКАЛЬНОЙ РАЗРАБОТКИ:
# ============================================
# DATABASE_URL=          # НЕ УСТАНАВЛИВАТЬ (используется SQLite)
# SUPABASE_URL=          # НЕ НУЖНО
# SENTRY_DSN=            # НЕ НУЖНО
# ENVIRONMENT=production  # НЕ МЕНЯТЬ!
```

### `web-ui/.env.local` файл (Frontend)

```bash
# Локальная разработка - Backend URL
NEXT_PUBLIC_API_URL=http://localhost:8000
```

---

## 🧪 ЛОКАЛЬНОЕ ТЕСТИРОВАНИЕ

### Запуск всех тестов

```bash
# Backend тесты
pytest tests/ -v

# Frontend тесты
cd web-ui
npm run test

# E2E тесты
npm run test:e2e
```

### Проверка работоспособности

```bash
# Health check
curl http://localhost:8000/api/health

# Регистрация пользователя
curl -X POST http://localhost:8000/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{"email":"test@example.com","password":"Test123!"}'

# Логин
curl -X POST http://localhost:8000/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"email":"test@example.com","password":"Test123!"}'
```

---

## 🔧 СКРИПТЫ ДЛЯ ЛОКАЛЬНОЙ РАЗРАБОТКИ

### `start_local.sh` - Запуск локальной разработки

```bash
#!/bin/bash
# Запуск backend и frontend для локальной разработки

echo "🚀 Запуск локальной разработки..."

# Проверка виртуального окружения
if [ ! -d "venv" ]; then
    echo "❌ Виртуальное окружение не найдено. Создайте его: python3 -m venv venv"
    exit 1
fi

# Проверка .env
if [ ! -f ".env" ]; then
    echo "❌ .env файл не найден. Скопируйте .env.example в .env"
    exit 1
fi

# Проверка ENVIRONMENT
if grep -q "ENVIRONMENT=production" .env; then
    echo "⚠️  ВНИМАНИЕ: ENVIRONMENT=production в .env!"
    echo "   Для локальной разработки должно быть ENVIRONMENT=development"
    read -p "Продолжить? (y/N): " -n 1 -r
    echo
    if [[ ! $REPLY =~ ^[Yy]$ ]]; then
        exit 1
    fi
fi

# Создать директории
mkdir -p data logs

# Запуск backend
echo "📦 Запуск backend на http://localhost:8000..."
source venv/bin/activate
python api/server.py &
BACKEND_PID=$!

# Ждем запуск backend
sleep 3

# Запуск frontend
echo "🎨 Запуск frontend на http://localhost:3000..."
cd web-ui
npm run dev &
FRONTEND_PID=$!

echo ""
echo "✅ Локальная разработка запущена!"
echo "   Backend:  http://localhost:8000 (PID: $BACKEND_PID)"
echo "   Frontend: http://localhost:3000 (PID: $FRONTEND_PID)"
echo ""
echo "Для остановки нажмите Ctrl+C"

# Ожидание сигнала
trap "kill $BACKEND_PID $FRONTEND_PID; exit" INT TERM
wait
```

### `stop_local.sh` - Остановка локальной разработки

```bash
#!/bin/bash
# Остановка всех локальных процессов

echo "🛑 Остановка локальной разработки..."

# Остановка backend
pkill -f "python api/server.py"
pkill -f "uvicorn"

# Остановка frontend
pkill -f "next dev"
pkill -f "npm run dev"

echo "✅ Все процессы остановлены"
```

### `reset_local.sh` - Сброс локальной базы данных

```bash
#!/bin/bash
# Сброс локальной базы данных (ОСТОРОЖНО!)

echo "⚠️  ВНИМАНИЕ: Это удалит всю локальную базу данных!"
read -p "Продолжить? (y/N): " -n 1 -r
echo

if [[ ! $REPLY =~ ^[Yy]$ ]]; then
    exit 1
fi

# Остановка сервера
pkill -f "python api/server.py"

# Удаление базы данных
rm -f data/history.db
rm -f data/history.db-journal

echo "✅ Локальная база данных удалена"
echo "   При следующем запуске будет создана новая"
```

---

## 🚫 ЧТО НЕ ДЕЛАТЬ ДЛЯ ЛОКАЛЬНОЙ РАЗРАБОТКИ

### ❌ НЕ устанавливать:

- `DATABASE_URL` - не нужен, используется SQLite
- `SUPABASE_URL` - не нужен для локальной разработки
- `ENVIRONMENT=production` - должно быть `development`
- `SENTRY_DSN` - не нужен для локальной разработки

### ❌ НЕ запускать:

- `deploy_production.sh` - только после тестов
- `railway up` - только для production
- `vercel --prod` - только для production

### ❌ НЕ коммитить:

- `.env` - всегда в `.gitignore`
- `data/history.db` - локальная база данных
- `venv/` - виртуальное окружение

---

## ✅ ЧЕКЛИСТ ПЕРЕД ДЕПЛОЕМ

Перед деплоем на production убедитесь:

- [ ] Все локальные тесты проходят (`pytest tests/ -v`)
- [ ] Frontend тесты проходят (`cd web-ui && npm run test`)
- [ ] E2E тесты проходят (`cd web-ui && npm run test:e2e`)
- [ ] Локальная разработка работает без ошибок
- [ ] Все изменения закоммичены
- [ ] `.env` не содержит production секретов
- [ ] `ENVIRONMENT=development` в локальном `.env`

**Только после этого:** Запустить `./deploy_production.sh`

---

## 📚 ДОПОЛНИТЕЛЬНЫЕ РЕСУРСЫ

- **Локальная установка:** Этот документ
- **Production деплой:** `DEPLOYMENT_GUIDE.md`
- **Troubleshooting:** `TROUBLESHOOTING.md`
- **API документация:** http://localhost:8000/docs (после запуска)

---

## 🆘 ПРОБЛЕМЫ И РЕШЕНИЯ

### Проблема: Backend не запускается

```bash
# Проверить Python версию
python --version  # Должна быть 3.11+

# Переустановить зависимости
pip install -r requirements.txt --upgrade

# Проверить .env файл
cat .env | grep ENVIRONMENT  # Должно быть development
```

### Проблема: Frontend не подключается к Backend

```bash
# Проверить .env.local
cat web-ui/.env.local  # Должно быть NEXT_PUBLIC_API_URL=http://localhost:8000

# Проверить что backend запущен
curl http://localhost:8000/api/health
```

### Проблема: База данных заблокирована

```bash
# Остановить все процессы
./stop_local.sh

# Удалить lock файл
rm -f data/history.db-journal
```

---

**Последнее обновление:** 2025-01-05  
**Версия:** 1.0.0  
**Статус:** ✅ Готово для локальной разработки

