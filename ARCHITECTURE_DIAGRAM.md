# 📐 Documentation Analyzer - Architecture Diagram

## 🏗️ System Architecture

```
┌─────────────────────────────────────────────────────────────────────┐
│                          USER INTERFACE                              │
│                    http://localhost:3000                             │
└─────────────────────────────────────────────────────────────────────┘
                                 │
                                 │ HTTP Requests
                                 ▼
┌─────────────────────────────────────────────────────────────────────┐
│                      FRONTEND (Next.js 14)                           │
├─────────────────────────────────────────────────────────────────────┤
│  Dashboard Page          Upload Page          Results Page           │
│  (/doc-analyzer)         (/doc-analyzer/new)  (/doc-analyzer/[id])  │
│                                                                       │
│  • Stats Display         • URL Input           • Tabbed Interface    │
│  • Document List         • Type Selector       • AI Explanations     │
│  • Filters               • Examples            • SQL Code Blocks     │
│  • Actions               • Submit              • Copy to Clipboard   │
└─────────────────────────────────────────────────────────────────────┘
                                 │
                                 │ REST API Calls
                                 ▼
┌─────────────────────────────────────────────────────────────────────┐
│                      API LAYER (FastAPI)                             │
│                    http://localhost:8000                             │
├─────────────────────────────────────────────────────────────────────┤
│                    Doc Analyzer Router                               │
│                 /api/doc-analyzer/*                                  │
│                                                                       │
│  POST   /documents                  → Create & analyze               │
│  GET    /documents                  → List all                       │
│  GET    /documents/{id}             → Get specific                   │
│  DELETE /documents/{id}             → Delete                         │
│  GET    /documents/{id}/analysis    → Get results                    │
│  POST   /documents/{id}/analyze     → Manual trigger                 │
│  POST   /schemas/generate-sql       → Generate SQL                   │
│  GET    /stats                      → Statistics                     │
└─────────────────────────────────────────────────────────────────────┘
                                 │
                                 │ Business Logic
                                 ▼
┌─────────────────────────────────────────────────────────────────────┐
│                    ANALYZER MODULES (Python)                         │
├─────────────────────────────────────────────────────────────────────┤
│                                                                       │
│  ┌─────────────────┐  ┌──────────────────┐  ┌──────────────────┐  │
│  │ OpenAPI Analyzer│  │ Analysis Engine  │  │ Schema Generator │  │
│  │                 │  │                  │  │                  │  │
│  │ • Fetch spec    │  │ • AI explanations│  │ • Type mapping   │  │
│  │ • Parse JSON    │  │ • Claude 3.5     │  │ • SQL generation │  │
│  │ • Parse YAML    │  │ • Bilingual      │  │ • Index creation │  │
│  │ • Extract data  │  │ • Summaries      │  │ • CRUD queries   │  │
│  └─────────────────┘  └──────────────────┘  └──────────────────┘  │
│                                                                       │
│  ┌──────────────────────────────────────────────────────────────┐  │
│  │              Base Analyzer (Abstract)                         │  │
│  │  • Common database operations                                │  │
│  │  • Status tracking                                           │  │
│  │  • Result saving                                             │  │
│  └──────────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────┘
           │                      │                      │
           │                      │                      │
           ▼                      ▼                      ▼
┌──────────────────┐  ┌──────────────────┐  ┌──────────────────────┐
│  External API    │  │  Claude AI API   │  │  PostgreSQL Database │
│                  │  │                  │  │                      │
│  • OpenAPI URLs  │  │  • Explanations  │  │  • doc_sources       │
│  • Swagger Specs │  │  • Summaries     │  │  • doc_analyses      │
│  • JSON/YAML     │  │  • Simple terms  │  │  • doc_endpoints     │
└──────────────────┘  └──────────────────┘  │  • doc_schemas       │
                                             │  • doc_generated_*   │
                                             │  • doc_exports       │
                                             └──────────────────────┘
```

---

## 📊 Data Flow Diagram

```
┌──────────────┐
│   User       │
│ enters URL   │
└──────┬───────┘
       │
       ▼
┌──────────────────────────────────────────────────────────────┐
│ 1. Frontend: POST /api/doc-analyzer/documents               │
│    { name, type, source_url, analyze_immediately: true }     │
└──────────────────────────────┬───────────────────────────────┘
                               │
                               ▼
┌──────────────────────────────────────────────────────────────┐
│ 2. Router: Create document record                            │
│    Status: PENDING → Save to doc_sources table              │
└──────────────────────────────┬───────────────────────────────┘
                               │
                               ▼
┌──────────────────────────────────────────────────────────────┐
│ 3. Background Task: Start analysis                           │
│    Status: PROCESSING                                        │
└──────────────────────────────┬───────────────────────────────┘
                               │
                               ▼
┌──────────────────────────────────────────────────────────────┐
│ 4. OpenAPI Analyzer: Fetch & Parse                          │
│    • Fetch from URL                                          │
│    • Parse JSON/YAML                                         │
│    • Extract endpoints                                       │
│    • Extract schemas                                         │
└──────────────────────────────┬───────────────────────────────┘
                               │
                               ▼
┌──────────────────────────────────────────────────────────────┐
│ 5. Analysis Engine: AI Processing                            │
│    For each endpoint:                                        │
│      → Send to Claude API                                    │
│      → Get simple explanation                                │
│    For each schema:                                          │
│      → Send to Claude API                                    │
│      → Get schema explanation                                │
│    Generate overall summary                                  │
└──────────────────────────────┬───────────────────────────────┘
                               │
                               ▼
┌──────────────────────────────────────────────────────────────┐
│ 6. Schema Generator: Create SQL                              │
│    For each schema:                                          │
│      → Map types (OpenAPI → PostgreSQL)                      │
│      → Generate CREATE TABLE                                 │
│      → Add indexes                                           │
│      → Add comments                                          │
└──────────────────────────────┬───────────────────────────────┘
                               │
                               ▼
┌──────────────────────────────────────────────────────────────┐
│ 7. Save Results to Database                                  │
│    • Save endpoints to doc_endpoints                         │
│    • Save schemas to doc_schemas                             │
│    • Save analysis to doc_analyses                           │
│    • Update status: COMPLETED                                │
└──────────────────────────────┬───────────────────────────────┘
                               │
                               ▼
┌──────────────────────────────────────────────────────────────┐
│ 8. Frontend: Display Results                                 │
│    GET /api/doc-analyzer/documents/{id}/analysis             │
│    • Show endpoints with AI explanations                     │
│    • Show schemas with generated SQL                         │
│    • Show summary                                            │
│    • Enable copy-to-clipboard                                │
└──────────────────────────────────────────────────────────────┘
                               │
                               ▼
                         ┌──────────┐
                         │   User   │
                         │ reviews  │
                         │ & copies │
                         │   SQL    │
                         └──────────┘
```

---

## 🔄 Process Flow

### 1. Document Upload
```
User Input → Validation → Database Insert → Return ID
```

### 2. Analysis (Background)
```
Trigger → Fetch URL → Parse → AI Analysis → SQL Generation → Save → Complete
```

### 3. Results Viewing
```
Request → Query DB → Format Response → Display UI
```

---

## 🗄️ Database Schema

```sql
┌─────────────────┐
│  doc_sources    │  ← Main table (documents)
├─────────────────┤
│ id              │ UUID PRIMARY KEY
│ name            │ VARCHAR(255)
│ type            │ VARCHAR(50) 'openapi', 'pdf', etc.
│ source_url      │ TEXT
│ status          │ VARCHAR(20) 'pending'|'completed'|'failed'
│ created_at      │ TIMESTAMP
└────────┬────────┘
         │
         │ one-to-many
         │
    ┌────┴────────────────────────────────┐
    │                                     │
    ▼                                     ▼
┌─────────────────┐              ┌─────────────────┐
│  doc_endpoints  │              │  doc_schemas    │
├─────────────────┤              ├─────────────────┤
│ id              │              │ id              │
│ doc_source_id   │              │ doc_source_id   │
│ method          │              │ schema_name     │
│ path            │              │ properties      │
│ ai_explanation  │              │ ai_explanation  │
│ parameters      │              │ generated_sql   │
└─────────────────┘              └─────────────────┘
```

---

## 🎯 Component Responsibilities

### Frontend
- ✅ User interface & interaction
- ✅ Form validation
- ✅ API communication
- ✅ Results visualization
- ✅ Error handling & loading states

### API Router
- ✅ Request/response handling
- ✅ Validation (Pydantic)
- ✅ Background task scheduling
- ✅ Error handling
- ✅ Database coordination

### Analyzers
- ✅ Document fetching
- ✅ Parsing (JSON/YAML)
- ✅ Data extraction
- ✅ Status management

### Analysis Engine
- ✅ AI API communication
- ✅ Explanation generation
- ✅ Summary creation
- ✅ Fallback handling

### Schema Generator
- ✅ Type mapping
- ✅ SQL generation
- ✅ Index creation
- ✅ Comment generation

### Database
- ✅ Persistent storage
- ✅ Data integrity
- ✅ Relationship management
- ✅ Query optimization

---

## 🔐 Security Layers

```
┌────────────────────────────────────────┐
│ 1. Input Validation                    │
│    • URL format validation             │
│    • Type checking                     │
│    • Size limits                       │
└────────────────────────────────────────┘
┌────────────────────────────────────────┐
│ 2. Authentication (Future)             │
│    • JWT tokens                        │
│    • User permissions                  │
│    • Rate limiting                     │
└────────────────────────────────────────┘
┌────────────────────────────────────────┐
│ 3. Database Security                   │
│    • Parameterized queries             │
│    • SQL injection prevention          │
│    • Connection pooling                │
└────────────────────────────────────────┘
┌────────────────────────────────────────┐
│ 4. API Key Protection                  │
│    • Environment variables             │
│    • Never in code                     │
│    • Rotation support                  │
└────────────────────────────────────────┘
```

---

## 📈 Scalability Design

### Horizontal Scaling
```
         ┌─────────┐
         │ Load    │
         │ Balancer│
         └────┬────┘
              │
    ┌─────────┼─────────┐
    │         │         │
    ▼         ▼         ▼
┌───────┐ ┌───────┐ ┌───────┐
│ API   │ │ API   │ │ API   │
│ Node 1│ │ Node 2│ │ Node 3│
└───┬───┘ └───┬───┘ └───┬───┘
    │         │         │
    └────────┬┴─────────┘
             │
             ▼
    ┌────────────────┐
    │   PostgreSQL   │
    │ (with replicas)│
    └────────────────┘
```

### Async Processing
```
API Request → Background Task → Worker Pool → Database
     ↓                                           ↑
  Immediate                               Results saved
   Response                                    later
```

---

## 🔧 Technology Stack

### Backend
- **Language:** Python 3.11
- **Framework:** FastAPI
- **Database:** PostgreSQL (asyncpg)
- **AI:** Anthropic Claude 3.5 Sonnet
- **Validation:** Pydantic
- **Async:** asyncio, BackgroundTasks

### Frontend
- **Framework:** Next.js 14
- **Language:** TypeScript
- **Styling:** Tailwind CSS
- **Routing:** App Router
- **State:** React Hooks

### Infrastructure
- **Database Pool:** asyncpg (2-10 connections)
- **HTTP Client:** httpx (async)
- **Parsing:** PyYAML, json
- **Validation:** openapi-spec-validator

---

## 📊 Performance Characteristics

### Response Times
```
Endpoint              Avg Time    Max Time
--------------------  ----------  ----------
POST /documents       <100ms      <500ms
GET /documents        <50ms       <200ms
GET /analysis         <100ms      <500ms
Background analysis   15-40s      60s
```

### Resource Usage
```
Memory:      ~200MB per worker
CPU:         Low (mostly I/O wait)
Database:    ~10 queries per analysis
API calls:   1-50 (depends on endpoint count)
```

---

## 🎨 UI Component Tree

```
App
└── Layout
    └── Navigation
        └── Doc Analyzer Link
            │
            ├── Dashboard Page (/)
            │   ├── Stats Cards (4x)
            │   ├── Filters
            │   └── Document Table
            │       └── Document Row
            │           ├── Status Badge
            │           └── Action Buttons
            │
            ├── Upload Page (/new)
            │   ├── Form
            │   │   ├── Name Input
            │   │   ├── Type Selector
            │   │   ├── URL Input
            │   │   └── Submit Button
            │   ├── Examples Section
            │   └── Tips Section
            │
            └── Results Page (/[id])
                ├── Tab Navigation
                ├── Summary Tab
                │   ├── AI Summary
                │   └── Stats
                ├── Endpoints Tab
                │   └── Endpoint List
                │       └── Endpoint Card
                │           ├── Method Badge
                │           ├── Path
                │           ├── AI Explanation
                │           └── Description
                └── Schemas Tab
                    └── Schema List
                        └── Schema Card
                            ├── Schema Name
                            ├── AI Explanation
                            ├── Fields List
                            ├── SQL Code Block
                            └── Copy Button
```

---

## 🔍 Error Handling Flow

```
Error Occurs
    │
    ├─ Network Error
    │    └─> Retry logic (3 attempts)
    │         └─> Still fails? → Set status FAILED
    │
    ├─ Parsing Error
    │    └─> Log details → Set status FAILED
    │         └─> Save error message for user
    │
    ├─ AI API Error
    │    └─> Use fallback explanations
    │         └─> Continue with basic analysis
    │
    └─ Database Error
         └─> Rollback transaction
              └─> Return error to user
```

---

**Architecture designed for:** Production-scale, High availability, Easy maintenance

**Last Updated:** January 8, 2025
