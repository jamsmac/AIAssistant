# 🔧 Исправление ошибки Vercel: Serverless Function Size

## Проблема
```
Error: A Serverless Function has exceeded the unzipped maximum size of 250 MB
```

## Решение

### 1. Конфигурация Next.js (`next.config.ts`)
- ✅ Добавлена оптимизация импортов пакетов
- ✅ Исключение больших optional зависимостей из серверного bundle
- ✅ Оптимизация webpack bundle

### 2. Конфигурация Vercel (`vercel.json`)
- ✅ Настроены параметры serverless функций
- ✅ Оптимизация build процесса

### 3. Исключение файлов (`.vercelignore`)
- ✅ Исключены тесты, документация, примеры
- ✅ Исключены development файлы

### 4. NPM конфигурация (`.npmrc`)
- ✅ Оптимизация установки зависимостей

## Дополнительные рекомендации

### Если проблема сохраняется:

1. **Проверить зависимости:**
   ```bash
   cd web-ui
   npm ls --depth=0
   ```

2. **Использовать только production зависимости:**
   ```bash
   npm install --production
   ```

3. **Альтернатива: использовать Edge Runtime**
   - Для страниц, которые не требуют Node.js API
   - Добавить `export const runtime = 'edge'` в страницах

4. **Разделить приложение:**
   - Использовать API Routes только где необходимо
   - Переместить тяжелую логику в отдельный сервис

## Проверка размера

После деплоя проверь размер в Vercel Dashboard:
- Settings → Functions
- Смотри размер каждой serverless функции

