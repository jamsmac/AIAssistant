# ⚡ БЫСТРЫЙ СТАРТ - ЛОКАЛЬНАЯ РАЗРАБОТКА

**Время установки:** 5 минут  
**Требования:** Python 3.11+, Node.js 18+

---

## 🚀 Установка за 3 шага

### 1. Клонирование и настройка

```bash
git clone <your-repo-url>
cd autopilot-core

# Создать виртуальное окружение
python3 -m venv venv
source venv/bin/activate

# Установить зависимости
pip install -r requirements.txt
```

### 2. Настройка переменных окружения

```bash
# Создать .env файл
cp .env.example .env

# Сгенерировать SECRET_KEY
python -c "import secrets; print(secrets.token_urlsafe(32))"
# Скопируйте вывод и вставьте в .env:
# SECRET_KEY=ваш-ключ-здесь

# Убедитесь что ENVIRONMENT=development
echo "ENVIRONMENT=development" >> .env
```

### 3. Запуск

```bash
# Установить frontend зависимости
cd web-ui && npm install && cd ..

# Запустить всё одной командой
./start_local.sh
```

**Готово!** Откройте http://localhost:3000

---

## 📝 Минимальная конфигурация

Для работы достаточно установить в `.env`:

```bash
ENVIRONMENT=development
SECRET_KEY=ваш-сгенерированный-ключ
GEMINI_API_KEY=ваш-ключ  # или любой другой AI API ключ
```

Остальное работает по умолчанию:
- ✅ SQLite база данных (создается автоматически)
- ✅ Локальный сервер на порту 8000
- ✅ Локальный frontend на порту 3000

---

## 🛑 Остановка

```bash
./stop_local.sh
```

Или нажмите `Ctrl+C` в терминале где запущен `./start_local.sh`

---

## 🔄 Сброс базы данных

```bash
./reset_local.sh
```

⚠️ **Внимание:** Это удалит все локальные данные!

---

## 📚 Подробная документация

- **Полное руководство:** [LOCAL_DEVELOPMENT_GUIDE.md](LOCAL_DEVELOPMENT_GUIDE.md)
- **Troubleshooting:** [TROUBLESHOOTING.md](TROUBLESHOOTING.md)
- **API документация:** http://localhost:8000/docs (после запуска)

---

## ✅ Проверка работы

```bash
# Health check
curl http://localhost:8000/api/health

# Должен вернуть: {"status":"healthy",...}
```

---

**Готово к работе!** 🎉

