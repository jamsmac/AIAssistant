# NPM configuration for Vercel deployment
# Note: devDependencies ARE needed for build time (TypeScript, etc.)
# They are automatically excluded from the final deployment bundle

package-lock=true
legacy-peer-deps=false















